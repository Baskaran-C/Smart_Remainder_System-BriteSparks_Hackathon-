package com.example.SmartRemainderSystem.entity.enums;

public enum ActivityType {
    APP_OPENED,           // User opened the web app
    REMINDER_SEEN,        // User clicked/viewed a reminder
    REMINDER_CONFIRMED,   // User confirmed via reminder link
    LOCATION_SHARED,      // User shared location
    APPOINTMENT_VIEWED    // User viewed their appointment details
}
